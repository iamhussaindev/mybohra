# npm configuration
# 
# Note: The deprecated config warnings (version-git-tag, version-commit-hooks, etc.)
# were fixed by updating npm to 11.7.0 or later. If you see these warnings again,
# update npm with: npm install -g npm@latest
loglevel=silent

